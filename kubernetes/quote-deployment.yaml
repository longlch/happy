---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quote-service-deployment
  namespace: development
  labels:
    name: quote-service-info
spec:
  replicas: 2
  selector:
    matchLabels:
      name: quote-service-info
  template:
    metadata:
      labels:
        name: quote-service-info
    spec:
      containers:
        - name: quote-container
          image: datawire/quote:0.5.0
          ports:
            - containerPort: 8080
