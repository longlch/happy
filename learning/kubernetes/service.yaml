---
apiVersion: v1
kind: Service
metadata:
  name: demo-service
  namespace: development
spec:
  selector:
    app: quote-service-info
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer